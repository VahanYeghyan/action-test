- name: Workflow for app

on:
   push:
      branches: [ main ]
   pull_request:
      branches: [ main ]

jobs:
    deploy:
          runs_on: ubuntu_latest

    steps:
         - name: build app
           run: docker
